import type { LoreEntryDefinition } from '@shared/types/lore';

export const factionHistoryLore: Record<string, LoreEntryDefinition> = {
  bunker_logs: {
    title: 'Bunker Command Logs',
    text: 'Operation Last Light was initiated to preserve key personnel. But something went wrong in Bunker 7.',
    category: 'Faction',
  },
};